<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/map_frag">

    <fragment
        android:id="@+id/map_frag"
        android:name="com.thecoder.maps.ui.FragmentMaps"
        android:label="Maps"
        tools:layout="@layout/fragment_maps" >



    </fragment>

    <fragment
        android:id="@+id/saved_frag"
        android:name="com.thecoder.maps.ui.FragmentSaved"
        android:label="Saved"
        tools:layout="@layout/fragment_saved" >

        <action android:id="@+id/to_maps"
            app:destination="@id/map_frag"
            />
    </fragment>

</navigation>